# 混合检索

混合检索（Hybird Search）结合稀疏向量和密集向量检索，旨在同时利用稀疏向量的关键词精准匹配和密集向量的语义匹配能力，提高检索效果。

## 稀疏向量 vs 密集向量

* 稀疏向量（别称：词法向量），基于词频统计的传统信息检索方式的数学表示，主要包含TF-IDF(Term Frequency-Inverse Document Frequency) 、BM25(Best Matching 25)等。维度极高，但绝大多数维度为零。稀疏向量检索主要利用词袋模型，通过计算词频和逆文档频率，得到一个稀疏向量。

* 密集向量（别称：语义向量），